// buildingConfig.js
// Содержит статическую конфигурацию для каждого типа зданий в приложении.
// Эта информация используется для отображения зданий (иконка, начальный размер),
// их ограничений (лимит на количество), классификации (тип альянса, игрока, специальное),
// а также для специфических свойств, таких как размер области влияния, возможность переименования,
// изменения размера или особый цвет фона.

// Глобальный объект window.appBuildingConfig для доступа из JSX без импортов,
// когда используется Babel Standalone.
// Ключи объектов здесь должны совпадать со значениями в window.appConstants.BUILDING_TYPES.

window.appBuildingConfig = {
    // --- Альянсовые здания ---
    [window.appConstants.BUILDING_TYPES.FORTRESS]: {
        icon: '🏰',      // Иконка для визуального представления
        size: 3,        // Базовый размер на сетке (N x N ячеек)
        areaSize: 15,   // Размер области влияния (0, если нет)
        limit: 1,       // Максимальное количество таких зданий на карте (-1 для безлимитного)
        type: 'alliance',// Внутренняя классификация типа здания
        canRename: false, // Флаг, можно ли переименовывать это здание
        isResizable: false // Флаг, можно ли изменять размер этого здания
    },
    [window.appConstants.BUILDING_TYPES.OUTPOST]: {
        icon: '🚩', size: 2, areaSize: 10, limit: 5, type: 'alliance',
        canRename: false,
        isResizable: false
    },
    [window.appConstants.BUILDING_TYPES.HELLGATES]: {
        icon: '👹', size: 3, areaSize: 0, limit: 1, type: 'alliance',
        canRename: false,
        isResizable: false
    },
    [window.appConstants.BUILDING_TYPES.HOSPITAL]: {
        icon: '🏥', size: 2, areaSize: 0, limit: 1, type: 'alliance',
        canRename: false,
        isResizable: false
    },
    [window.appConstants.BUILDING_TYPES.FARM]: {
        icon: '🌾', size: 2, areaSize: 0, limit: 1, type: 'alliance',
        canRename: false,
        isResizable: false
    },
    [window.appConstants.BUILDING_TYPES.WAREHOUSE]: {
        icon: '🏭', size: 2, areaSize: 0, limit: 1, type: 'alliance',
        canRename: false,
        isResizable: false
    },

    // --- Здания игроков ---
    [window.appConstants.BUILDING_TYPES.CASTLE]: {
        icon: '🏯', size: 2, areaSize: 0, limit: -1, type: 'player',
        canRename: true,  // Замки игроков можно переименовывать
        isResizable: false // Замки обычно имеют фиксированный размер
    },

    // --- Специальные/Зональные объекты ---
    [window.appConstants.BUILDING_TYPES.DEADZONE]: {
        icon: '⚠️',      // Иконка для панели инструментов и, возможно, для самого элемента
        size: 1,        // Начальный размер при размещении (1x1 ячейка).
                        // Фактические width и height хранятся в объекте самого здания в состоянии,
                        // так как они могут изменяться пользователем.
        areaSize: 0,    // Мертвые зоны обычно не имеют области влияния
        limit: -1,      // Безлимитное количество
        type: 'special',// Классификация
        canRename: true, // Мертвые зоны можно называть (например, "Область Васи", "Зона фарма")
        isResizable: true, // Флаг, указывающий, что это здание может изменять размер
        bgcolor: 'rgba(144, 238, 144, 0.45)' // Цвет фона для отображения на сетке
    }
};